# Add a new feature

Goal: implement the requested change safely and leave behind passing checks.

Acceptance criteria:
- All required checks pass.
- No blocking audit findings remain.
- The code change is contained to the smallest reasonable diff.

Constraints:
- Do not rewrite unrelated files.
- Preserve existing behavior outside the requested scope.

Out of scope:
- Large refactors unrelated to the requested task.

Notes:
- Replace this example with a real task-specific plan before running.
